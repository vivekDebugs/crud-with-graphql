{
	"name": "customers-api",
	"version": "1.0.0",
	"description": "Simple CRUD operations using GraphQL",
	"main": "server.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"dev:server": "nodemon server.js",
		"json:server": "json-server --watch db.json --port 4000"
	},
	"author": "Vivek Kumar",
	"license": "ISC",
	"dependencies": {
		"axios": "^0.24.0",
		"express": "^4.17.1",
		"express-graphql": "^0.12.0",
		"graphql": "^16.1.0",
		"json-server": "^0.17.0"
	}
}
